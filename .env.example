# Copy this file to .env (or .env.local) for local development against the
# admin panel's GitHub OAuth login. Not needed to run the public site itself.
#
# See README.md "Environment variables" for how to create the GitHub OAuth
# App these values come from.

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
